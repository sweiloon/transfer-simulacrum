# Netlify SPA redirect rules
# This ensures all routes are handled by React Router
/* /index.html 200

# Optional: API redirects (if needed in future)
/api/* https://vbmmeaddhdefxfgtigzv.supabase.co/:splat 200